{
  "name": "num-photography-club-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "http://Dasha0309.github.io/Suudreg-photography-club-web",
  "scripts": {
    "optimize-images": "node scripts/optimize-images.js",
    "build": "npx shx rm -rf dist && npx shx mkdir dist && npx shx cp index.html dist/ && npx shx cp favicon.svg dist/ && npx shx cp -r styles dist/ && npx shx cp -r assets dist/ && npx shx cp -r components dist/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node": "^25.8.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.98.0",
    "sharp": "^0.33.5"
  }
}
